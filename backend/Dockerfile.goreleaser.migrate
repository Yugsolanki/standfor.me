# Database Migrations

FROM alpine:3.20

ARG TARGETARCH

RUN apk add --no-cache ca-certificates tzdata

COPY linux/${TARGETARCH}/standforme-migrate /usr/local/bin/standforme-migrate
COPY backend/migrations/ /migrations/

ENTRYPOINT ["standforme-migrate"]